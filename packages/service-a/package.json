{
    "name": "@monodemo/service-a",
    "private": true,
    "version": "1.0.0",
    "workspaces": {
        "nohoist": ["**"]
      },
    "scripts": {
        "build": "npm run clean && npm run compile",
        "clean": "rm -rf ./dist && rm -rf tsconfig.build.tsbuildinfo",
        "compile": "tsc -b tsconfig.build.json",
        "test": "jest",
        "start": "npx ./dist/index.js"
    },
    "dependencies": {
        "@monodemo/common": "1.0.0"
    },
    "devDependencies": {
        "@types/jest": "^26.0.18",
        "jest": "^26.6.3",
        "ts-jest": "^26.4.4",
        "typescript": "^4.1.2"
    }
}
